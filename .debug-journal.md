# Debug Journal — issue 313 CI stale candidate
Started: 2026-09-16
Goal: Reproduce the CI-only ground-cache stale failure and isolate each motion fixture.

## Artifacts to revert
- [ ] Temporary CI-only diagnostic logging in `test/verify-studio-agent-motion.mjs`
- [ ] `.debug-journal.md`

## Findings
- The fixture's `readTarget` closes over a per-case `state`; it does not read `state.cast`.
- `fixture()` creates a new state, but candidate owners were retained in the shared `owners` array until the outer `finally`.
